library(httptest)
library(httr)

baseurl <- Sys.getenv("CLONE")
user <- Sys.getenv("C_CLONE")
pass <- Sys.getenv("C_PASS")

# start_capturing()
# pull_events(baseurl = baseurl, program_id = "Ymn7RO0lkf7")
# stop_capturing()
